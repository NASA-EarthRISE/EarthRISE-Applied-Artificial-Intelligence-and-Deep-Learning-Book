# Zenodo API token - get one at:
#   Production: zenodo.org > Settings > Applications > Personal access tokens
#   Sandbox:    sandbox.zenodo.org > Settings > Applications > Personal access tokens
# Required scopes: deposit:write, deposit:actions
ZENODO_TOKEN=your_token_here
